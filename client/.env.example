# CodeShield Client — Environment Variables
# Copy to .env and fill in values

# API base URL (empty = use Vite proxy to localhost:4000)
VITE_API_URL=

# Optional: client-side API key (sent as x-api-key header)
VITE_API_KEY=
